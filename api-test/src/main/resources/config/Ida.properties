#---------------------------------- End point(s) relative URLs ----------------------------------#

#---------------------------------- Modifiable Properties ----------------------------------------------------------#

#------------------------ Environment URLs and Database Connections ------------------------#

# Keycloak URL.
keycloak-external-url = https://iam.qa-base.mosip.net

# PostgreSQL URLs for audit and partner databases.
audit_url = jdbc:postgresql://qa-base.mosip.net:5432/mosip_audit
partner_url = jdbc:postgresql://qa-base.mosip.net:5432/mosip_ida

# Database server for connections.
db-server = qa-base.mosip.net
db-port = 5432

#------------------------ secrets and passwords  ------------------------#

#------------------------ Keycloak Passwords ------------------------#
# Used for Keycloak authentication.
keycloak_Password = 

#------------------------ PostgreSQL Database Passwords ------------------------#
# Credentials for connecting to Postgres databases.
audit_password = cNIE2mC7Wf
partner_password = cNIE2mC7Wf
postgres-password = cNIE2mC7Wf

#-------- Client Secret Keys ----------#
# These keys are used for various services, make sure to update the values as required when running locally.

mosip_partner_client_secret = EJSqWeGAx7e4G7hh
mosip_pms_client_secret = Ei8Qiv24ek2vlyf8
mosip_resident_client_secret = 4Iz28rPkGFsAr36B
mosip_idrepo_client_secret = 7vwZeyseYgqqw8z7
mosip_reg_client_secret = dfT3rPsm1jK2jI3x
mosip_admin_client_secret = uTHNbr8eFO0fxU42
mosip_hotlist_client_secret = MAJfrm53JudPTj0y
mosip_regproc_client_secret = 2USbhxjayz096XHc
mpartner_default_mobile_secret = G34tntGCGQuZpgwH
mosip_testrig_client_secret = zwNlQC8fWg62RRqB
AuthClientSecret = 4Iz28rPkGFsAr36B
mosip_crvs1_client_secret = 


#-------- Generic Configuration ----------#

# Enable or disable debugging mode (yes/no).
enableDebug = yes

# Whether to use pre-configured OTP (true/false).
usePreConfiguredOtp = true

# Mock Notification Channels (email/phone/email,phone).
mockNotificationChannel = email,phone


#------------------------ Mosip Components Base URLs ------------------------#
# Define base URLs for different components if required.
# Example: 
# mosip_components_base_urls = auditmanager=api-internal.qa-base.mosip.net;idrepository=api-internal.qa-base.mosip.net;authmanager=api-internal.qa-base.mosip.net;resident=api-internal.qa-base.mosip.net;partnermanager=api-internal.qa-base.mosip.net;idauthentication=api-internal.qa-base.mosip.net;masterdata=api-internal.qa-base.mosip.net;idgenerator=api-internal.qa-base.mosip.net;policymanager=api-internal.qa-base.mosip.net;preregistration=api-internal.qa-base.mosip.net;keymanager=api-internal.qa-base.mosip.net;mock-identity-system=api.qa-base.mosip.net
# Feel free to add more components as needed.
mosip_components_base_urls =

#------------------------ Module Name Pattern ------------------------#
# Define module name pattern if required.
# Example: 
# moduleNamePattern = (mimoto|resident)
# Feel free to add more values as needed.
moduleNamePattern =


#------------------------ Uncomment for Local Run ------------------------#

# Path to the authentication certificates (if running locally, uncomment the below line and keep the value empty).
# authCertsPath =

# X-XSS-Protection: Controls the XSS (Cross-Site Scripting) filter in browsers.
# Values: (yes/no)
xssProtectionCheck = no


# Wait time (in milliseconds) after UIN generation in IDREPO before proceeding
delayTime = 90

# Watchdog timeout in minutes (max execution time before forced termination)
watchdogTimeoutMinutes = 120

# Maximum number of retry attempts for UIN availability check during OTP generation
uinGenMaxLoopCount = 20